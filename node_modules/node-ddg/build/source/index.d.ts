export declare enum SafeSearchEnum {
    'OFF' = -2,
    'MODERATE' = -1,
    'ON' = 1
}
export interface Result {
    body: string;
    title: string;
    url: string;
}
export interface SearchOptions {
    maxResults?: number;
    query: string;
    safeSearch?: SafeSearchEnum;
    secure?: boolean;
}
export interface Protocols {
    http: string;
    https: string;
}
/**
 * @function search
 * @description Returns the title, body and link of searched queries from DuckDuckGo.
 * @param args Search options
 * @param args.maxResults Max results to return, defaults to `5`
 * @param args.query Search query, required
 * @param args.safeSearch Safe Search setting as defined in https://duckduckgo.com/params, defaults to `MODERATE`
 * @param args.secure Whether to use HTTPS or HTTP, defaults to `true`
 * @license MIT
 */
export declare function search(args: SearchOptions): Promise<Result[]>;
